server:
  extraFlags:
    - web.enable-remote-write-receiver
    - web.enable-lifecycle

  ingress:
    enabled: true
    ingressClassName: traefik
    hosts:
      - thor.frec.kr

alertmanager:
  enabled: false
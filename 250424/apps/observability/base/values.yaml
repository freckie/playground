server:
  extraFlags:
    - web.enable-remote-write-receiver
    - web.enable-lifecycle

  ingress:
    enabled: true
    ingressClassName: traefik
    hosts:
      - observe.thor.frec.kr

alertmanager:
  enabled: false